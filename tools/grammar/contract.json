{
  "schema": "wenyan.grammar.contract.v1",
  "grammar": "wenyan.relations.reporting.v1",
  "name": "Reporting frame (speaker / addressee / quoted speech)",
  "purpose": "Give the relation layer a grammar that actually occurs in real text, so the corpus-grounding gate (G5) has a reachable path. The previous grammar occurs in 0 of 8208 corpus passages, so nothing learned from it can ever be checked against real text.",
  "module": "src/inscription/relations-reporting.ts",
  "discipline": {
    "identicalTo": "wenyan.relations.v1",
    "rules": [
      "the model stores ONLY evidence (supervised labelled examples)",
      "surviving role permutations are recomputed by replay() on every read and update",
      "an incomplete check (fuel exhausted) adds no constraint",
      "a contradicting label throws; the caller may retry explicitly",
      "no match, too many matches, or out-of-scope input all yield Unknown"
    ],
    "notModified": "src/inscription/relations.ts is untouched — see duplicationDebt"
  },
  "constructions": [
    { "id": "wei-quote", "frame": "<A>謂<B>曰：「<S>」", "verb": "謂", "corpusPassages": 144 },
    { "id": "wen-quote", "frame": "<A>問<B>曰：「<S>」", "verb": "問", "corpusPassages": 182 },
    { "id": "gao-quote", "frame": "<A>告<B>曰：「<S>」", "verb": "告", "corpusPassages": 28 },
    { "id": "yu-quote", "frame": "<A>語<B>曰：「<S>」", "verb": "語", "corpusPassages": 13 }
  ],
  "slots": 3,
  "segmentation": {
    "problem": "The experimental grammar delimited every slot with 「」. Real text offers no such help for the two entity slots, so boundaries must be declared rules rather than assumed.",
    "slot0": "maximal run immediately left of the frame verb; no sentence punctuation (，。；：「」？！、); length 1..10",
    "slot1": "maximal run immediately right of the frame verb and left of 曰; same character rule; length 1..10",
    "slot2": "content of the 「…」 immediately right of 曰; length 1..300",
    "measuredSpanLengths": { "median": 2, "max": 9 },
    "measuredAmbiguity": "32 of 399 matching passages contain more than one frame occurrence and return Unknown(ambiguous-frame-occurrence) rather than picking one"
  },
  "roleInterpretation": {
    "declared": { "slot0": "agent (speaker)", "slot1": "recipient (addressee)", "slot2": "theme (quoted speech)" },
    "status": "DECLARED, then pinned by supervised evidence — the module starts from all 6 permutations per construction and eliminates 5 of them by refutation. If supervision contradicts this reading, the evidence wins.",
    "labelProvenance": "tools/grammar/seed-reporting.mjs records who declared each label; the label is a declaration, not a discovery"
  },
  "failureModes": [
    { "condition": "no frame occurrence", "result": "Unknown(outside-reporting-grammar)" },
    { "condition": "more than one frame occurrence in the passage", "result": "Unknown(ambiguous-frame-occurrence)" },
    { "condition": "construction has more than one surviving permutation", "result": "Unknown(ambiguous-role-mapping)" },
    { "condition": "text longer than 2048 units", "result": "Unknown(outside-reporting-grammar); corpus maximum is 1617, so this excludes nothing today" },
    { "condition": "contradicting supervised label", "result": "throw contradictory evidence; no update is committed" }
  ],
  "measured": {
    "source": "knowledge/grammar/measurement.json, regenerated by tools/grammar/measure.mjs",
    "corpusPassages": 8208,
    "reachUniqueParse": 367,
    "reachRate": 0.0447,
    "ambiguous": 32,
    "overLengthLimit": 0,
    "answerableWithEvidence": 367,
    "answerableRate": 0.0447,
    "evidenceRecords": 8,
    "perWork": { "hanfeizi": 82, "guanzi": 67, "zhuangzi": 65, "mozi": 44, "liezi": 29, "book-of-rites": 28, "analects": 20, "xunzi": 16, "mencius": 14, "book-of-documents": 2 },
    "coverageGate": "G5 floor is 0.01; this grammar reaches 0.0447, so the gate is now reachable. The previous grammar reaches 0.0."
  },
  "measuredButNotAdmitted": [
    {
      "frame": "<A>曰：「<S>」",
      "slots": 2,
      "corpusPassages": 3102,
      "rate": 0.3779,
      "whyNotAdmitted": "It has two slots, and the machinery maps a construction onto exactly three roles with all three required. Admitting it would mean changing learnRelations' semantics to allow partial role assignments, which this step is explicitly not allowed to do.",
      "status": "Measured and declared, deliberately unused. It is the single largest coverage gain available (37.8% of passages) and it is being left on the table on purpose until a separate contract defines what a missing role means. Pulling it in by inventing a recipient would be exactly the kind of unlicensed inference the dialogue contract forbids."
    }
  ],
  "duplicationDebt": {
    "what": "The evidence/replay discipline is duplicated between relations.ts and relations-reporting.ts rather than extracted into a shared core.",
    "why": "Extracting it would modify the verified module of the reviewed patch. That refactor should be its own reviewed change, not a side effect of adding a grammar.",
    "risk": "The two copies can drift. Mitigation: tools/grammar/selftest.mjs asserts that both modules obey the same discipline on the same cases.",
    "recommendation": "Fold both onto a parameterised core when the 32-example cap and projection strategy are reviewed (step 3)."
  },
  "nonclaims": [
    "not parsing: this is a declared regex frame with declared boundary rules, not a syntactic analysis of classical Chinese",
    "not understanding: a matched frame says the text has this shape, nothing about what it means",
    "the entity slots are unquoted runs, so a boundary rule can be wrong; the ambiguity and span measurements are reported rather than hidden",
    "reach is an upper bound that depends on segmentation only; it is not accuracy",
    "no claim that the declared speaker/addressee/speech reading is the correct linguistic analysis — it is a declared convention, pinned by supervision and falsifiable by it",
    "4.47% coverage is low by design: the frame was chosen because it occurs, not because it is common"
  ]
}
